import React from 'react';
import { mount } from 'enzyme';
import Demo from './custom-filtering-algorithm';

describe('BS3: custom filtering algorithm demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
