import React from 'react';
import { mount } from 'enzyme';
import Demo from './table-row-template';

describe('BS4: table cell template demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
