import React from 'react';
import { mount } from 'enzyme';
import Demo from './table-header-cell-template';

describe('MUI: table header cell template demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
