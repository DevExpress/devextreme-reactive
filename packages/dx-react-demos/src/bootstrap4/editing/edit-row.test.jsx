import React from 'react';
import { mount } from 'enzyme';
import Demo from './edit-row';

describe('BS4: edit row controlled demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
