import React, { useState, useRef } from 'react';<%&additionalImports%>
<%&imports%>
import {
  Grid,
  VirtualTable,
  TableHeaderRow,
  Toolbar,
} from '@devexpress/dx-react-grid-<%&themeName%>';
import {
  Plugin,
  Template,
  TemplatePlaceholder,
} from '@devexpress/dx-react-core';

import {
  generateRows,
  defaultColumnValues,
} from '../../../demo-data/generator';

const getRowId = row => row.id;

<%&body%>

export default () => {
  const [columns] = useState([
    { name: 'id', title: 'ID' },
    { name: 'name', title: 'Name' },
    { name: 'gender', title: 'Gender' },
    { name: 'city', title: 'City' },
    { name: 'car', title: 'Car' },
  ]);
  const [rows] = useState(generateRows({
    columnValues: { id: ({ index }) => `id_${index}`, ...defaultColumnValues },
    length: 100000,
  }));
  const [tableColumnExtensions] = useState([
    { columnName: 'id', width: 75 },
  ]);
  const [id, setId] = useState(0);
  const vtRef = useRef();

  const topRowChange = (newId) => {
    setId(newId);
  };

  const scrollToRow = (rowId) => {
    vtRef.current.scrollToRow(rowId);
  };
  console.log(id)

  return (
    <<%&wrapperTag%><%&wrapperAttributes%>>
      <Grid
        rows={rows}
        columns={columns}
        getRowId={getRowId}
      >
        <VirtualTable
          columnExtensions={tableColumnExtensions}
          ref={vtRef}
          onTopRowChange={topRowChange}
        />
        <TableHeaderRow />
        <Toolbar />
        <ScrollPanel
          id={id}
          setId={setId}
          scrollToRow={scrollToRow}
        />
      </Grid>
    </<%&wrapperTag%>>
  );
};
