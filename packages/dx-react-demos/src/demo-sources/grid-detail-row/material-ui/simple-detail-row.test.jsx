import React from 'react';
import { mount } from 'enzyme';
import Demo from './simple-detail-row';

describe('MUI: simple detail row demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
