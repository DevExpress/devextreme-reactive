name: Tests

on:
  push:
  pull_request:

jobs:
  CI_Workflow:
    runs-on: ubuntu-latest
    steps:
    - name: Get sources
      uses: actions/checkout@v2

    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '15'

    - run: echo "Workflow is added!"
