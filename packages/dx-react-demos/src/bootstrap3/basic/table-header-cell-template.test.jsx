import React from 'react';
import { mount } from 'enzyme';
import Demo from './table-header-cell-template';

describe('BS3: table header cell template demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
