import React from 'react';
import { mount } from 'enzyme';
import Demo from './detail-row-controlled';

describe('MUI: detail row controlled demo', () => {
  it('should work', () => {
    mount(<Demo />);
  });
});
