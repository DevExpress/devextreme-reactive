import { GridViewport } from '../../types';

/** @internal */
export const emptyViewport: GridViewport = {
  columns: [[0, 0]],
  rows: [0, 0],
  headerRows: [0, 0],
  footerRows: [0, 0],
  viewportTop: 0,
  viewportLeft: 0,
};
