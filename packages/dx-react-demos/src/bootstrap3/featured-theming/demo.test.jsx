import React from 'react';
import { mount } from 'enzyme';
import Demo from './demo';

describe('BS3 featured: theming', () => {
  it('should work', () => {
    mount(
      <Demo />,
    );
  });
});
